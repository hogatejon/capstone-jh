import { SpotsAvailablePipe } from './spots-available.pipe';

describe('SpotsAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new SpotsAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
