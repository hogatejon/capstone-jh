import { OrgFilterPipe } from './org-filter.pipe';

describe('OrgFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrgFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
