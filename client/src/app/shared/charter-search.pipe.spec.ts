import { CharterSearchPipe } from './charter-search.pipe';

describe('CharterSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CharterSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
